mixin img(img, alt)
    img(src=`img/${img}`, alt= alt)

mixin list(list, classList)
    ul(class= classList)
        each item in list
            li: a(href=item.url)= item.title

mixin cat_item(item)
    tr
        td= item.title
        td= `&gt; ${item.alc}%`
        td= item.cal
        td= `${item.ob} мл`
        td= item.sum + 'Р.'

mixin form_el(id, holder)
    .order-form-group
        label(for= id)= holder
        input(type="text" name= id id= id)


mixin new_item(item)
    div.new-item
        h3= item.title
        div.new-item__ingridient
            Table
                tr
                    td  Алк.
                    td= `&lt; ${item.alc}%`
                tr
                    td Ккал
                    td= item.cal
            p.new-item__ingridient__compound= item.sostav
        img(src=`img/${item.img}` alt=item.title)
        p= item.description
        div.new-item__button
            a.new-item__button--details.btn(href="#") Подробнее
            a.new-item__button--buy.btn(href="#") Купить
            b.new-item__button--price= `${item.price} Р.`